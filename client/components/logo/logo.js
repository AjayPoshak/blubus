import React from 'react';
import styled from 'styled-components';

const TextWrapper = styled.h1`
	color: #fff;
	font-size: 25px;
	font-weight: 500;
`;

const Logo = () => (
	<TextWrapper>BluBus</TextWrapper>
);
export default Logo;
